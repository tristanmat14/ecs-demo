#pragma once

// we need to create the data structure that will store the archetypes.

// How do we want to use it? Something like componentManager.query<Types...>();

struct ArchetypeTest {
    template<typename C>
    void add(C component, archetypeID) {
        archetype ID
    }   

};

